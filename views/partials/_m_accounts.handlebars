{{#if accounts}}
  <div  class="input_left_box flex_c_c">
    <div class="btn_circle flex_c_c br60">
     <i class="bi bi-chevron-down"></i>
      <div class="a_c_menu a_container_br">
        {{#each accounts}}
          <div accesskey="{{gateway}}" class="padd5-a usedAccount">{{account}}</div>
        {{/each}}
      </div>
    </div>
  </div>
{{/if}}